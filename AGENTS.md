# sauce-homepage

## Project

- GitHub Pages 정적 홈페이지
- 기존 루트 `index.html`과 `assets/`는 명시적 승인 없이 수정하지 않는다.
- 비교 시안은 `concepts/` 아래에 서로 독립적으로 작성한다.

## Verification

- 데스크톱 1440×900과 모바일 390×844에서 확인한다.
- 콘솔 오류, 깨진 에셋, 키보드 탐색, `prefers-reduced-motion`을 확인한다.
- 작업 전후 루트 `index.html`과 `assets/`가 변경되지 않았는지 확인한다.

## Commands

별도 빌드 과정이 없는 정적 HTML/CSS/JavaScript 프로젝트다.
